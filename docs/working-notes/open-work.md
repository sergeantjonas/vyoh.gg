# Open work index

**Status:** Index — canonical list of tracked arcs and their next action.

One-line pointers into the owning notes. Read this first when scoping the next session — it answers "what's still open across the working notes" without re-scanning each doc.

**Maintenance rule:** when an item ships, descopes, splits, or promotes, edit this file in the same commit that lands the change. The detail lives in the owning note; this index only carries a one-line pointer and the current state. Entries here should never grow beyond a sentence.

**Status-header convention:** every note in `working-notes/` carries a `**Status:** <Active|Shipped|Parked|Reference|Index> — <one-line>` header right under the H1, updated in the same commit as state changes. Skim-scanning the folder should reveal active arcs without opening each doc. Fully-shipped notes whose planning detail no longer carries weight live in [archive/](archive/) instead of here.

**Companion index:** [parked.md](parked.md) carries the inverse — items deliberately set aside until a trigger condition holds. Read it when looking for "what could we revisit when the mood strikes."

---

## Tracked arcs — next action

### LoL surfaces

- **Match-depth Phase E** — **complete 2026-09-08**: full rune page panel (MD2, owner-only, Your Game tab) and the composite "Score-of-game" grade beside the recap badges both shipped. Nothing left on the match-depth roadmap; parked sub-items stay in [parked.md](parked.md). → [match-depth-roadmap.md](lol/match-depth-roadmap.md)
- **Queue identity: label → numeric `queueId`** — arc **shipped 2026-08-01**; `Match.queueType` is gone, every live queue has a label, and queue 710's `RANKED_PREMADE_5x5` ladder is captured and displayed alongside solo and flex (opt-in for statistics). Two follow-ups outstanding. The app has not been exercised in a browser across the migration. And the live surface is **open-world**: `live-game-poller` passes `gameQueueConfigId` through unfiltered and `queueLabel` falls back to `Queue ${id}`, so whatever spectator-v5 returns reaches the live halo / favicon dot / now-playing strip under some label. Whether **LoL Classic** ever arrives there is unobserved, not established — Classic is absent from match history entirely, and nobody has read spectator-v5 during a Classic game. Owner to observe next time they play it. Filtering by name is ruled out, since the arc forbids a Classic entry in `QUEUE_TYPES`, so the only code-side fix is inverting the live surface to closed-world — which trades a dark live surface for every new Riot mode until the map catches up, and that is the worse everyday failure. → [queue-id-migration.md](lol/queue-id-migration.md)
- **LP forecast Phase LP3 (personal linear fit)** — LP2 (calibration) + LP2.6 (threshold tuning) + LP2.7 (slot warning suppression) shipped 2026-05-20. Agurin's calibration first showed two stuck-positive signals (tilt/champ); LP2.6 re-threshold-ed them against the player's overall WR. The follow-up calibration then exposed slot warnings as anti-predictive (29% directional accuracy on 41 fires) — LP2.7 dropped the warning polarity. LP2.5 (per-signal weighting) parked until a re-run shows variance per signal. LP3 (personal linear fit) still long-tail. → [lp-forecast.md](lol/lp-forecast.md)

### Cross-cutting

- **Dormant chapter ranking** — analysed and **shipped 2026-08-25**: a 25% completion gate (D1), the brief-launch guard extended over the unlock half of `freshest` (D2), and the review prompt ordered by recent playtime. **The completion gate was then replaced 2026-09-01** by the sharper signal the note left unscoped: the brief-launch floor now rises from 30 to 120 minutes when a game's achievement progress has been frozen for a year *while achievements remained* — so an unproductive launch re-dates the game to its last real progress instead of the game being deleted from the lane. The gate proved blunt by measurement: of the nine live candidates it dropped, only Cyberpunk was the target, and the other eight were real playthroughs with grindy or absent achievement sets (Binding of Isaac at 21 of **641**). Invisible collateral only because all eight last ran 2010–2023 — a three-hour replay of any of them would have deleted the game the owner had just played. Visible lane output unchanged; Cyberpunk now sits in the lane at its true 448-day date rather than being removed. One correction to the 08-25 verification below: D2 alone already sufficed to drop Cyberpunk (both observed benchmark launches, 14m and 25m, are under the *ordinary* 30-minute floor), so the gate's only unique effect was that collateral, and the raised floor ships as a stated backstop that fires on nothing today. Still open, unscoped: the parked "Greatest hits" chapter kind. Historical, kept for the reasoning (Cyberpunk's last unlock is 2025-06-10 against a 2026-07-07 launch — fourteen months of launches with no progress; the refinement is that an unlock gap only means something *while achievements remain*, which is what separates benchmark use from a finished game re-launched later, and Silksong shows the false positive). **The sort order stays as it is:** `/` is a current-activity portrait, so the dormant lane's job is to be the closest thing to current among games that aren't, and ranking it by lifetime hours would pin a 434h Nightreign to the top of the Steam block months after it was last touched. That was this note's first proposal and it was wrong — `collectSteamSubjectCandidates` already records the same failure shipping once (a 3-minute Silksong re-launch beating a real RE2 playthrough) and prescribes a separate "Greatest hits" chapter kind for magnitude, explicitly *not* a tweak to subject scoring. Two real defects remain. **D1:** the lane cannot tell play from time-at-the-executable. Cyberpunk isn't abandoned — owner confirmed 2026-08-25 that its 36h are largely **in-game benchmark runs for hardware stress-testing**, a real playthrough is still pending, and it "doesn't need an exception to the logic". So the exception goes: the `unfeaturedAt` row is deleted when the gate ships, and the blocklist is a surface expected to empty out. Completion is bimodal here (20 games at 100% averaging 86h against 10 at ≤24% averaging 14h) so a 25% **gate** works where a completion *sort* never would; it excludes Cyberpunk for a partly wrong reason — benchmark hours earn no achievements, which looks identical to abandonment — but self-corrects once a real playthrough pushes completion past the line. Session shape would be the truer signal (3DMark shows it at a 2-minute median) but `SteamPlaySession` only begins when the player-state poller shipped, so it can diagnose nothing historical; dead end recorded. **D2:** a single round re-tops the lane — it is spliced in after `selectChapters` so it has no score floor at all, `BRIEF_LAUNCH_2W_MINUTES = 30` is a hair-trigger most rounds clear, and the guard is bypassable even under 30 minutes because it nulls only `lastPlayed` while `freshest = max(lastUnlock, lastPlayed)` reads the ungated unlock. **D6 (intended, not a defect):** chapter selection reads the *public* curation and `/` is not viewer-aware, so a newly-purchased game arrives `hiddenAt`-set from the poller and is dropped from both the steam-subject pool and `detectFirstTimeGames` — a 10-hour release-day binge is invisible on `/`, to the owner too, until the review prompt is answered. **Confirmed as the primary case by the owner 2026-08-25** (*"I don't want everyone to see I had a binge session of an adult game"*): the more the page would want to say about a new game, the likelier saying it is the wrong call, so the suppression scaling with engagement is the correct direction. Two things must therefore never be built — auto-publishing past an engagement threshold (fires hardest on exactly the games meant to stay back) and moving the review prompt to `/` (the current failure direction is *stays private*, the safe one). Ordering the prompt by recent engagement is still worth doing, to lead with the game the owner has a real decision about in either direction — smallest item here and independent of D1/D2. Invariant written into the note before any fix: a new game played 10h on release day must outrank the whole dormant lane, and it does — `baseSignal 10` vs floor 5 puts it in the *active* lane, which precedes dormant structurally. So `BRIEF_LAUNCH_2W_MINUTES` must **not** be raised as part of the D2 fix; the bug is the unlock bypass, not the threshold. Traps recorded: the 5h lifetime floor is inert (smallest real candidate is 7h) while the `appType` filter is what actually keeps Wallpaper Engine's 181h and 3DMark's 87h out. Falsifiable claim: the gate excludes Cyberpunk on its own, making its `unfeaturedAt` row deletable — unless it was really excluded for being *off-identity* (20 completions, heavily soulslike, one off-genre open-worlder), in which case the row stays and the blocklist is permanent rather than expected to empty. Decide that before implementing. → [dormant-chapter-ranking.md](cross-cutting/dormant-chapter-ranking.md)
- **App Phase 6 (optional)** — Mastery integration (the Riot mastery pull exists and feeds the live-game poller; no Champions-side surface yet), multi-account compare, live-tab audit. → [app-state-analysis.md](lol/app-state-analysis.md)
- **2026-06-11 structure + frontier audit** — fan-out index. CSS-platform arc C1–C9 complete 2026-06-14; W5 (queryOptions + loader prefetch) shipped with the Start migration 2026-07-27; A2 closed by owner-auth's boot-time `requireEnv` 2026-08-13. W1–W3 shipped (2026-09-02, 2026-09-06); A1 shipped 2026-09-06; W4, A3, A4 are in [parked.md](parked.md) with triggers. Feature candidates F1–F4 unchanged. → [audit-2026-06-11.md](cross-cutting/audit-2026-06-11.md)
- **Frontend-2026 KB expansion** — three new domain files decided 2026-06-12 (18-angular, 19-migrations, 20-data-visualization; AI-patterns deferred with trigger). File 20 compiled 2026-06-14 (with the vyoh Recharts audit); 18-angular and 19-migrations remain, one file per session, order 18 → 19. → [frontend-2026-kb-expansion.md](cross-cutting/frontend-2026-kb-expansion.md)
- **2026-06-12 visual-excellence audit** — session plan (V1+V2, V5, V8) shipped in full 2026-06-12: V1 sweep (`excludeRemakes`, `ChapterLabel`, `ChapterShell`, `EMPTY_FRAME_CLASS`), V2 conventions.spec assertions, V5 subtab + Steam-index `routeMeta`, V8 chart palette + `ChartTooltipShell`. V3 (patches identity) shipped 2026-06-12 — splash claim, frosted section cards, frost-safe mount cascade, perf budget row; owner eyes on the live page pending. V4 (match-detail subtab transition polish) + V6 (theme-derived focus-visible ring, cross-engine) shipped 2026-06-13. V11 shipped 2026-06-14. V7 (status-page polish) shipped 2026-09-05: layout skeleton, retryable unavailable state, `CardTitle` inside the chromed cards, reservoir bars in the method table, duration bands on the tick history, a live/polling pill that only says what the EventSource has reported, every zone on one `StatusCard` shell with pill rows or a bare divider table inside, and the admin tables aligned to the same cell rhythm and toggle shape. Remaining: V9/V10/V12 design decisions, unscoped. → [visual-excellence-audit-2026-06-12.md](cross-cutting/visual-excellence-audit-2026-06-12.md)
- **Detail routes: server-rendered content — done, container-probed** — the panel reaches the server render as of 2026-09-03 (`PanelLayer` in `slide-panel.tsx` renders in place before hydration, portals after), and `matches/$matchId` is **done**: its loader awaits on the server only (7.9 kB / 1.6 ms, fatal — the panel is the route), and reseeding `bodyReady` off a cold-arrival test instead of `supportsViewTransitions()` took it 1554 → 2304 characters while clearing the hydration mismatch that had the same cause. Six panel URLs verified clean in a real browser against six untouched controls. `library/$appid` is **done 2026-09-05**: the body gates on the game's library row, which came from the 664 kB owned-games list the priming rule keeps client-side, so the row got its own `GET /steam/game/:appid` (3.9 kB / 14 ms) and primes on the server only — fatal on an outage, tolerating a 404 as loader data so the owner's hidden games still deep-link — with the description and achievement list tolerated beside it. 124 → **3387** characters of `<main>` text, hydration clean in headless Chromium against the dev server. Priming the description surfaced a `DOMParser` call in the about block's render path — unreachable on the server only while nothing primed it — now a string pass over the sanitizer's output. `champions/$championKey` is **done 2026-09-05**: its body is derived from the 361 kB match window (182 kB for the main champion alone), so instead of priming it the api computes the same `detail` and `overall` aggregates — `computeChampionDetail` and `computeTrendSummary` moved to shared with `ChampionStats`, `RolePosition` and `CHAMPION_DETAIL_WINDOW` — and returns them with the champion extras (12.6 kB / 12 ms), which the loader primes on the server only, fatal. The page prefers its window when it has one and reads the extras otherwise; the window-driven heatmaps render nothing until the window lands. 200 → **648** characters of `<main>` text, hydration clean in headless Chromium against the dev server for a main and a one-game champion. `champion-hero.tsx` reads `useViewTransitionsSupported()`; `CountUp` starts settled on a cold arrival via `useHydratedSync()` so the server emits the final numbers; the Motion `initial` transforms in the body, `WinRateBar` and the patch strip are gated on `useHydrated()`. **The last item closed 2026-09-06:** the panel no longer docks at `top: 0` for the pre-hydration frame — each section declares its header height via `SectionShell`'s `headerDockPx` (128 LoL, 104 Steam) and the panel reads it as a CSS fallback until the measured `--account-header-h` lands. The container probe ran 2026-09-06 and came back clean for all three (bodies served, dock var present, no hydration error, palette opens). → [tanstack-start-migration.md § What still does not render server-side](cross-cutting/tanstack-start-migration.md)

### Steam surfaces

- **Achievement rarity drift — closed; only the parked mature-library beat remains** — R1 shipped 2026-08-07: [global-rarity.service.ts](../../apps/api/src/steam/achievements/global-rarity.service.ts) now reads the outgoing percentages inside its transaction and appends to `SteamAchievementRarityHistory` whenever one moved at Steam's own one-decimal precision, seeded with an origin row per achievement from the 9,085 existing rarity rows. Nothing reads it, deliberately — the value accrues with elapsed time, not with more code. R2 is a diagnostic script in `apps/api/src/scripts` reporting span / endpoints / slope per achievement, and it is the **gate** on R3: build the UI beat when R2 names a game with a real slope in the rare band, never on spec. The probe behind the arc says why that gate exists — 91 of 857 rows moved in a work-week, but **the rarest twelve owner-unlocked achievements the trophy case actually shows (1.9%–5.1%) moved 0.000pp**, so *"0.3% when you got it, 1.9% now"* will not happen for the existing library and shouldn't be re-pitched. It lands only on games bought near release, going forward — and one arrived four days later: **Beast of Reincarnation moved 0.0 → 0.9–2.0% in six days** (released 8-03, polled 8-05, re-read live 8-11), in the rare band. **R2 shipped 2026-08-12** as `probe-rarity-drift.ts`, and **its first reading does not clear the gate**: of 9,085 series, only 520 have a second observation, and all of them move ≤0.30pp — the rare band's largest is one 0.1pp quantum (`Tomb Raider — No Stone Left Unturned`, 8.2% → 8.3%). The probe leads on coverage rather than movement for exactly this reason: 8,565 single-point series produce the same silence as a flat market and mean the opposite thing. **The second reading, taken the same day after the `2026-08-12 00:12:07Z` boot drained it, changes the verdict.** Beast of Reincarnation moved on all 46 series, 45 of them past the visibility bar, topping out at **16.1% → 46.2% (+30.10pp) in 7 days** with relative leaders to 26×. But it released 8-03, so that is a launch curve, not settled drift — and splitting the 566 two-point series by release age separates the populations completely: **launch window 45 of 46 visible, max +30.10pp; mature 0 of 512 visible across 58 games, max +0.30pp; unknown release date 0 of 8**. So **launch-window drift is established and the mature library is not resolved either way** — an 8-day span puts a 0.1–0.3pp move on Steam's own precision floor, where quantization noise and ~3pp/year are indistinguishable, and "settled titles don't drift" would be an overclaim from a window too narrow to measure one. **R3 is unblocked on data and now needs a scoping call**: a launch-window beat has a live, dramatic dataset today but only fires on newly-played releases, while a whole-library drift beat would be blank for 58 of 60 games and stays gated on elapsed time. R2 was extended in the same session to report the split, because its pooled verdict printed `Gate CLEARED` off one nine-day-old game — true and materially misleading; unknown-release-date games get their own cohort so an unenriched new release can't contaminate the settled population. **The poller's cadence was split 2026-08-13** (`RARITY_LAUNCH_MAX_AGE_MS` = 1 day inside a 60-day `LAUNCH_WINDOW_MS`, 7 days outside it): the pitched *"0.3% when you got it"* sentence finally has data — **33 of Beast's 42 unlocks postdate its first observation** — but every series holds only **two** points against a 20pp/week curve, so the honest claim is the vaguer *"1.4% → 24.1% while you played it"*, and an unsampled curve is gone for good. The two cohorts are selected in **separate queries** because they can't share an ordering — `lastRarityCheckedAt asc` sorts a daily-polled title behind every weekly one and past the cap — and the launch query is skipped outright when the cohort is empty, since an unfiltered query at the 24-hour cutoff selects the whole library (the first cut did exactly that; the spec pins it). This fixes selection, not sampling: the 05:30 tick still fires on a sleeping dev box, so it means a restart costs a launch title one day of resolution instead of seven. That same reading exposed a display defect, **fixed 2026-08-11**: Steam reports a literal `0` for any share below its one-decimal floor, which the trophy case rendered as `0.0%` beside four achievements the owner had unlocked. `formatRarityPercent` / `formatRarityPercentEditorial` now render anything that would round to 0.0 as `<0.1%` — the predicate is `< 0.05`, not `=== 0`, so the invariant is "no surface prints 0.0%" — across all four display sites, three of which were formatting their own percentage instead of using the shared badge. Ranking is unchanged, since `0` genuinely is the rarest bucket Steam can express. **Third reading 2026-09-02, R3 decided.** Twenty days after the cadence split: 3,786 two-point series (566 before), launch cohort **99 of 99 visible across two owner-played day-one titles** — Beast of Reincarnation +36.90pp over 28 days on 12 observations, Mortal Shell II +31.50pp over 12 days on 5 — and the mature cohort's `Gate CLEARED on settled titles` line is true and misleading a second time: its 460 visible series are mostly Isaac's 0.5–1.2pp community creep outside the rare band, and inside it the movement is Nioh 3 rising off a literal 0.0 (a content drop, not drift) plus DOOM: The Dark Ages at +2.1pp in 27 days. No settled rare-band series reached 2× from a non-zero origin. **Decision: R3 is the launch-window beat**, a third `steam-moment` type `LAUNCH_RARITY_DRIFT` gated on a captured launch curve rather than on the window still being open, so it stays on `/` until recency decay retires it; the mature-library beat is parked behind "a settled rare-band series ≥ 5pp or 2× from a non-zero origin". The note carries the full chunk plan — shared deriver → api detector → web beat, with types, thresholds, tests and the shipped pattern each chunk copies — plus an optional R2b probe fix that excludes from-the-floor series from the mature verdict. **Chunk 1 shipped 2026-09-02**: `deriveLaunchDrift` and the descriptor types in `packages/shared`, and the `moment-accent.ts` case pulled forward out of chunk 3 because that `switch` turned out to be exhaustive with no `default`. **Chunks 3 and 2 shipped the same day, and the live check then turned the last of the work from implementation into a calibration call — closed the same day as R3b.** The beat and the achievement cluster were being compared by score on scales that disagreed (a capped drift beat at 15 against a capped cluster at 40), so `LAUNCH_DRIFT_SIGNAL_FACTOR` went 0.5 → 4/3 to make both ceilings 40, and same-appid moments now dedup by score in `recap-subjects.service.ts` ahead of the moment ↔ subject pass. **Live on `/`:** Beast of Reincarnation renders as `LAUNCH_RARITY_DRIFT` at 14.86 having beaten its own cluster at 13.46, headline `Closest Companion` 3.2% → 38.4% over 12 readings. Mortal Shell II and Mortal Shell keep their clusters. **Visual review done 2026-09-02 in Firefox and closed**: text all correct, but the curve shipped with no legibility treatment and vanished into the splash, so `Sparkline` gained an opt-in `outline` under-stroke (the `paint-order: stroke` analogue for a drawn mark) and the beat's stroke went 1.5 → 2. A `drop-shadow` filter was rejected as layer-promoting, which would have owed a `recap` paint-budget re-probe. **R3 is closed, and R2b shipped 2026-09-06** (`hasSettledOrigin()` keeps rises off Steam's zero floor out of the mature verdict; each cohort line prints its rare-band count). What remains on this arc is the parked mature-library beat and its re-check condition. → [achievement-rarity-drift.md](steam/achievement-rarity-drift.md)
- **Description image rendering — A7 (optional backfill probe)** — A1–A5 shipped 2026-05-26 end-to-end: rendered `about_the_game` HTML now drives game-detail descriptions with inline `<video>` clips routed through a new range-streaming description-asset proxy, lazy-populated on first view. A7 (eager backfill so first views skip the cold-fetch round-trip) is the only pending item; defer until a marquee surface shows a visible cold-window. → [description-image-rendering.md](steam/description-image-rendering.md)
- **Steam Sessions — complete 2026-09-14/15, chunks 0–6 and the copy pass** — a seventh Steam tab `/steam/sessions` reading the 68 observed `SteamPlaySession` rows and their unlocks: beat-model hero for the last sitting, visx timeline with brush, hour heatmap, per-game strips, records, and an off-camera ledger for unlocks no session saw. The `4h+` bucket is non-empty as of 2026-09-14, so the portrait's cards 4 and 8 land here. `/steam/sessions` is live with the hero and records; the strip took a seventh tab without a breakpoint change, measured. → [play-sittings.md](steam/play-sittings.md)
- **Steam Player Portrait — shipped; cards 4 and 8 wait on api uptime** — every planned chunk (0, 1, 2, 3, L, P, 4, 5) shipped 2026-08-01→06 and lives on `/steam` under a **Portrait** header. Only cards 4 and 8 and the single-session cohort remain, and all three read session boundaries our poller only sees while the api is up. Chunk 0 measured the data: nothing prunes either table, but `SteamPlaySession` holds 28 rows across 5 games and misses launches outright, because the poller lives in the api process and only runs on the dev box — so **cards 4 and 8 and the single-session cohort are out of scope**, and the engagement floor's `≥2 launch days` rescue clause is inert (it rescues 0 of 14 tasted games). **Re-measured 2026-08-06, and the sample size was never the real objection:** `/home/session-lengths` buckets the 25 closed Steam sessions as `<30m: 12 · 30m–1h: 4 · 1h–2h: 6 · 2h–4h: 3 · 4h+: **0**`, against 2,903 lifetime hours with 434 in one game. The `4h+` bucket is **structurally** empty — the dev server is up while the owner works and down while they play, so the long sittings are exactly the ones never observed — which means card 4's marathon count would render `0` confidently and falsely rather than imprecisely. LoL shows the contrast on the same endpoint: 185 sessions, 4 over four hours, on a smaller time investment, because those are reconstructed from match timestamps Riot hands over retroactively. The deploy tooling shipped 2026-07-27, so the blocker is **uptime, not hosting work**; the gate to re-check is a non-empty `4h+` bucket, not a row count. Snapshots are fine: gaps don't matter against a cumulative counter, and the 90-day window becomes real on 2026-08-12 — build it adaptive and label the span actually used. The cohorts are the payoff: **195 owned / 60 meaningfully played / 121 never launched**, with 99.8% of 2,893 lifetime hours inside the cleared cohort. Chunk 1 shipped the allowlist + engagement helpers in `packages/shared/src/steam/portrait/`, verified against the live library — the fingerprint went from "Action, Singleplayer, Third Person" to "57% of your 2,356 hours sit in Souls-like, Action RPG, Third-Person Shooter". Two traps it closed: playtime must be *divided* across a game's matched tags rather than repeated per tag, and community meme tags (`Dating Sim` rank 20 on NIGHTREIGN, `Rhythm` rank 19 on Sekiro) are real genres no allowlist can reject — only the rank limit catches them. Chunk 2a shipped `GET /api/steam/portrait` on 2026-08-02 (lifetime fingerprint, recency fingerprint, engagement posture), and the probe now prints the endpoint's own answer next to its independent reading — the two agree. It also surfaced the thing chunk 2b has to design around: **the recency window is 80 days holding 3 games and 29 hours**, so every genre in it rests on one game — card 2 ships saying "not enough recent play to call a drift" rather than ranking noise, and re-earns its claim once the window holds real history. Chunks 2b and 2c shipped the same day, completing chunk 2: cards 1, 2, 3, 5 and 6 live on `/steam` under a **Portrait** header, with `portrait` and `platform-mix` primed in the route loader. Card 3 turned out to need api work — completion has to be joined against playtime for the ten-hour floor — and its numbers land the specced framing exactly: **34 of 186 owned games reach ten hours, 17 of those are at 100%, median completion 95%**. Card 5 could not use its specced copy at all: the owner's library is **100% Windows**, so it reads `Windows, exclusively` and spends its second line on the coverage gap instead (Steam attributes only 79% of lifetime playtime to any platform). Both superseded chips are deleted — `PlatformMixChip` by card 5, `LibraryCompositionChip` by card 6. Verified against the running api rather than the suite alone: 3,726 B in ~18 ms, and all five claims appear in the served HTML. Chunk 3a shipped the same day: `SteamPortrait` gained an `anti` block (tasted count/total/median + quickest abandons + tasted genre fingerprint, the single-achievement cohort, the coldest last-launch), with the selections beside the identity ones in `abandonment.ts`. It re-specced card 12 — `SteamOwnedGame.firstSeenAt` defaults to `now()` on insert, so it dates *our poller*, not the purchase, and the specced "on your shelf since 2019" would have been fabricated; the card reads `rtimeLastPlayed` instead and says **"Deus Ex: Human Revolution — 24 hours in it, untouched since July 2012"**. It also caught a trap for any future surface sorting on that column: Steam answers an **epoch sentinel of 1970-01-02** for some pre-cloud titles (Modern Warfare 2, against 410 recorded minutes), which an unguarded "oldest wins" ranking crowns every time. Measured lines for the remaining cards: **11 tasted games / 265 min / median 22**, quickest is NieR Replicant at **1 minute**, **6 games** in the single-achievement club, and the sharpest bounce line is **JRPG — tried 2, bounced off both**. Chunk 3b followed the same day: cards 7, 9 and 11 live on `/steam` under an **Anti-Portrait** header, riding the loader's existing prime, with all three claims in the served markup — *"11 games opened and given up on, for 4h 25m between them"*, *"NieR Replicant lasted 1 minute"*, *"You've opened 2 JRPG games and given up on both"*. Card 11 ranks by rate rather than volume, because 2-of-2 and 3-of-16 are the same "three-ish abandoned games" until the denominator is shown. Chunk 3c closed the half the same day with cards 10, 12 and 13 — *"6 games are sitting on exactly one unlocked achievement"*, *"Deus Ex: Human Revolution hasn't been launched since July 2012"*, and the closing *"You own 186 games, meaningfully played 55, finished 18. The gap is the hobby."* The served page corrected two things the suite passed: card 11's floor moved from two games **tried** to two games **abandoned**, because its third row was a true 50% resting on one dropped game, and card 10 moved off `formatPlaytime`, which rounds to whole hours and rendered 97 and 147 minutes identically as `2h`. **Chunk 3 is complete, and so is the layout pass that ran ahead of chunk 4** (L1–L5, 2026-08-02): the owner flagged the shipped page as long to scroll and flat to read, eleven cards of identical shape with no signal about which claim is the thesis. Each half now opens with an editorial hero band — **Souls-like** for the Portrait, **120 never opened** for the Anti-Portrait, each with a bar-row breakdown under it — over a column-flowed band of compact chips; cards 1 and 13 are deleted into the two heroes. Four things worth carrying forward. `CardShell` stopped claiming `h-full`, because whether a row shares a height is the grid's decision — and **a browser probe caught the one place that regressed**, the trends grid, which reaches its tiles through two wrappers so a stretched grid area only arrives if every link passes it on; no happy-dom test can see that. Density is now declared by the band via context rather than threaded as a prop through eleven cards. The chips flow in **CSS columns, not a grid**, because a grid aligns independent claims into rows and strands the odd one. And the pass came in *under* the route's paint budget — 26 layers / 67 ms raster against 30 / 100 before — while leaving the page ~320 px **taller**, which is the trade an editorial hero buys. **The portrait then moved to its own route** (`/steam/portrait`, a fifth Steam tab): the landing page's document halved to 20.3 kB and lost its loader entirely, the new route server-renders all 2,372 characters of its argument, and it baselines cheaper than the page it left (24–25 layers / ~53 ms). Two things worth carrying: the Steam tab order had **three** hand-kept copies and now has one, because a tab present in two of three does not fail — it slides the wrong way; and `/steam`'s raster floor went **up** when content was removed (67 → 129 ms), since the portrait bands had been pushing the trophy-case art below the fold. **Chunk 4 then closed the arc's last built surface** (4a 2026-08-05, 4b 2026-08-06): a **What to play** band between the two halves, scoring all 117 never-launched games against the lifetime fingerprint — *"Nioh 3 is the closest thing on your shelf to what you already play"*, *"706h into Souls-like and 11 you've never launched"*, *"Where Winds Meet got 49 minutes and stopped there"*. Scored against the **lifetime** fingerprint rather than the recency window the catalog specced, because that window holds three games and would recommend one recent purchase back at itself; swapping the argument is the only change needed when it fills. A candidate's score is the **share of the portrait its genres account for**, not a count of tag hits, and the age penalty counts back from the library's newest release rather than the clock — the score ships to the browser and is re-read at hydration, so `Date.now()` on both sides of that boundary is the bug the SSR convention is about. Two things the measurement changed. Ranking the sleeping genre by pile size crowns `Action` (112 waiting, **1%** of the portrait) and by share crowns whatever *Pick up next* already recommends, so it ships as **share × count**. And live, both single-game cards match on every genre they carry, so they had to be given different sentence shapes — plus both count indicators were repeating their own verdict and wrapping two of the three titles, which the browser showed and the suite could not. **Chunk 5 then closed the plan** (2026-08-06), scoped by reading every card's rendered copy off the live page at once — the only way the repetitions are visible, since each card is defensible alone. The page was stating its sharpest number **three times**: `119 never opened` was the Anti-Portrait masthead *and* the second line of both `Library posture` and `Tasted tier`. Both cards now spend that line on something they own. The same defect caught the new sleeping-genre card on its second pass, whose first cut opened with the Portrait masthead's own sentence. Two claims that named no population now do. The threshold pass **changed nothing, which is the result** — every gate was checked against its live output and holds — with one thing to know: `ANCIENT_PENALTY` cannot reach any rendered surface, since it applies only at zero genre overlap and `bestMatch` drops those candidates before ranking, so tuning it changes nothing on screen. **Every chunk in the plan has now shipped.** What's left is blocked, not unscoped: cards 4 and 8 and the single-session cohort all wait on the api running continuously long enough for the `4h+` bucket to stop being empty, and the owner still owes the 154-tag allowlist a skim (`Open World`, `Tactical`, `Sandbox` are the arguable calls). → [player-portrait.md](steam/player-portrait.md)
- **Game-detail enrichment — directions index** — trailers shipped 2026-05-29 (carousel + unified lightbox + Shaka player). Remaining backlog of per-game leads: personal-time storytelling, demo discoverability, supported-languages chip, in-Steam game notes, news feed (the per-game rarest-unlock card has shipped) — consolidated from api-surface-survey, library-card-enrichment, steam-integration. Also carries the pending structural question of whether to split the page into editorial-landing + playthrough-tab. → [game-detail-enrichment.md](steam/game-detail-enrichment.md)

### Pre-deploy / admin

- **Pre-launch sweep — gate list** — canonical ordering of what must land before public traffic, plus the standing prod-risk rules; assembled 2026-08-01. **Nine of thirteen gates closed as of 2026-09-13.** The remaining four are backups + a restore drill, DNS plus `VITE_API_URL` (now a GitHub Actions repository variable *as well as* a box `.env` line — CI bakes it into the image, and compose still interpolates the retained build args at `up`), branch protection, and error tracking — the last of those is repo-side and needs no box. The container log caps gate was added and closed 2026-09-13. The backup gate's repo-side half landed 2026-08-15 — `scripts/backup.sh`, `scripts/restore.sh`, `deploy/systemd/` — with the drill rehearsed locally at exact row-count parity; installing the timer and drilling on the box are box-side, and the off-box copy is deferred until there is a box to choose a target from. Launch is no longer blocked on the VPS alone: the remaining observability gate, error tracking, is repo-side and lands before it. → [pre-launch-sweep.md](ops/pre-launch-sweep.md)
- **Image pipeline — build in CI, pull on the box** — **scoped 2026-09-16; chunks 1 and 3 shipped the same day, chunk 2 is the launch prerequisite left.** GitHub Actions builds both images on every push to `main` (gated on the check job, `linux/amd64`, tagged `sha-<7>` + `main`, pushed to GHCR), and `deploy.sh` becomes manifest-check → narrowed rsync → `pull` → `up --no-build`. Exists because the box is now a netcup VPS 1000 G12 with 8 GB, not the Hetzner CX43: the 16 GB sizing rested solely on the on-box build spike, so this arc is what makes 8 GB valid. Same-sha tags also make the "deploy api and web together" rule structural, and rollback a tag. **Chunk 1 shipped 2026-09-16** as the `images` job: both pushes sit behind both probes — a `require("onnxruntime-node")` run against the api image, which is the only silent failure the `TARGETARCH` prune has, and a `/robots.txt` curl against the web one. **Verified green 2026-09-16** (run `35148907656` at `90e43de`): both packages publish and are anonymously pullable with no manual visibility flip, the pulled web image boots and serves `/robots.txt`, and the push pass rides the builder cache — 262 s / 181 s to build, 13 s / 8 s to push — so gating both pushes behind both smokes is free. The one owner step is real: the `VITE_API_URL` and `VITE_SITE_URL` repository variables, whose absence failed the first attempt in four seconds. `vars` is snapshotted at run creation, so setting one mid-run needs a job re-run to take effect. **Chunk 3 shipped 2026-09-16** — the runbook's step 0 is the netcup box as bought, step 1 adds the repository variables, step 5 pulls rather than builds, and § Sizing implications records that 8 GB is valid *because* builds are off-box, with the Hetzner table kept as the record of why the switch happened. **Chunk 2 shipped 2026-09-16**: `compose.prod.yaml` carries `image:` beside its retained `build:`, and `deploy.sh` refuses a tag `docker manifest inspect` cannot find before touching the box, ships only the ops surface, then `pull` + `up -d --no-build --wait` and writes `/srv/vyoh/.image-tag`. Verified against the published images — the real stack came up from GHCR with migrations and all three smoke targets at 200 — but proven on the box only at runbook step 5. Chunk 4 (`workflow_dispatch` deploy, GHCR retention) is post-launch. → [image-pipeline.md](ops/image-pipeline.md)
- ~~**`STEAM_API_KEY` optional in compose, required by the code**~~ — **found and closed 2026-09-16.** `compose.prod.yaml` declared `STEAM_API_KEY: ${STEAM_API_KEY:-}`, so compose would have brought the api up with an empty value, but `steam-client.service.ts` calls `requireEnv("STEAM_API_KEY")` at construction and Nest fails the whole bootstrap: `Error: Missing required env var: STEAM_API_KEY` — a crash loop on first deploy, not a degraded Steam half. Caught by running the *published* images as a real stack rather than by reading either file, since each is correct on its own; exactly the shape of the owner-auth gap in [pre-launch-sweep.md](ops/pre-launch-sweep.md) that hid a launch blocker for a week. Fixed to `:?`, matching the four owner-auth vars directly below it. **Still open as a question, not a bug:** `STEAM_GRIDDB_API_KEY` degrades gracefully in the same service while this one is fatal, and nothing has decided whether a Steam-less boot should be possible at all. → [hosting.md § 3](ops/hosting.md)

- **Error tracking (observability chunk 2)** — **scoped 2026-09-13, not started; the one repo-side launch gate left.** Decision made: hosted Sentry over self-hosted, because a tracker on the same VPS cannot report that the VPS is gone, and because the box's 8 GB is budgeted for tenants rather than an observability platform. Cheap to revisit — instrumentation targets Sentry's protocol, so GlitchTip or Bugsink later is a DSN change, not re-instrumentation. Seven hook points already exist and none are new code paths: the `Verdict` in `fallback-exception.filter.ts` (report on `log` present **and** `status >= 500`, not on `level === "error"` — 5xx and every Steam failure log at `warn`), `riot.exception-filter.ts` (registered after the catch-all, so it is the *only* filter a `RiotError` reaches — missing it would have left the whole LoL half silent), the `SyncJobRegistry` catch, the `node-adapter.ts` request catch where SSR render failures surface, the `ErrorBoundary` `onError` prop, the route error components, and the query/mutation cache handlers. **The risk is the redaction problem**, not the wiring: `redactSecrets()` scrubs query params in a message string, while a Sentry event also carries breadcrumbs, headers and stack locals, so a careless `beforeSend` re-opens F-5 one layer out. Sourcemaps are also absent (`vite.config.ts` sets no `sourcemap` key), which would silently make every web trace unreadable; the repo being public removes the usual objection to just shipping them. Four chunks. **E1a shipped 2026-09-13** (the shared scrubber — `redactSecrets` / `isSecretKey` / `redactSecretsDeep` in `packages/shared/src/redaction.ts`, 26 tests, registry switched off its private copy). **E1b shipped 2026-09-14 and needed no account**: `@sentry/nestjs` pinned at 10.73.0, `instrument.ts` imported second in `main.ts`, `captureException` at both api hook points, and an absent `SENTRY_DSN` disables the SDK so the wiring is inert until the value is set. Reporting keys on `status >= 500`, not on log level — a 503 logs at `warn`, so a level predicate would report no upstream failure at all. Verified against a local envelope receiver, not by reading code. **E2a shipped 2026-09-14** (SSR tier: `@sentry/node`, the node-adapter catch where render failures actually surface, and the TanStack client-disconnect filter in `beforeSend`). E2b browser and E3 sourcemaps + releases remain. Note the browser DSN is a **build argument** like `VITE_API_URL`, so it must exist before the first production web image is built. → [error-tracking.md](ops/error-tracking.md)
- **Observability floor** — **found 2026-09-12; a launch gate the sweep did not have.** Every other gate asks whether the app is correct on first public contact; this one asks whether anyone will know when it stops being. **Chunk 1 shipped 2026-09-13:** `compose.prod.yaml` carried no `logging:` block, so all three services used the uncapped `json-file` default; fixed with a shared `x-logging` anchor at 10m x 5 each. A companion healthcheck finding was refuted on review — both Dockerfiles have declared `HEALTHCHECK` since 2026-07-27 — and is recorded in the note rather than deleted, with the probe-design lesson that produced it. Chunk 2 is error tracking, which is wired nowhere in the workspace while [vnext-ideas.md](cross-cutting/vnext-ideas.md) itself calls it "Required for any public deployment"; the decision is made in [error-tracking.md](ops/error-tracking.md): hosted Sentry, because a tracker on the same VPS cannot report that the VPS is gone. Chunk 3 unparks API audit A3 (Pino + request ids) whose parked trigger, "the app is hosted", fires the day the box exists; it lands week one rather than pre-launch. Runtime response validation deliberately stays parked — error tracking is what produces the incident evidence that item's trigger waits on. → [observability-floor.md](ops/observability-floor.md)
- **API exposure audit** — **audit complete 2026-08-03; F-1 through F-21 fixed across 2026-08-03/05** (owner-allowlist choke point + lint, match/Steam upstream clamps, edge rate limiting, image-proxy traversal and redirect refusal, cache-key busting, Steam key redaction, `/og` caching + off-thread raster, classified status errors, wishlist-hero ownership clamp). **What remains:** F-22 is mitigated rather than structurally removed (the shared Steam reservoir is still one bucket, but the routes that drained it fastest no longer do), and cache-table eviction gates on verified backups. F-21's clamp gates on the in-memory wishlist rather than the `SteamWishlistAsset` table the plan named — reading the flow showed that table is only written for appids stale in the name cache, so a warm process writes nothing, while the wishlist cache the web itself derives the appid from is both authoritative and always populated. The audit also amended [owner-auth.md](ops/owner-auth.md) before that arc is written: session rows key on a SHA-256 `tokenHash` rather than the token, the generator is named explicitly, and expiry gains a 90-day absolute ceiling. The nginx config — rate-limit zones, `/og` cache block, cache key, headers — is committed, syntax-validated and behaviourally tested against a real nginx in a container. It needs no action while the project is pre-production; it lands with the rest of the deploy when hosting does, and the only thing to remember at that point is that `limit_req_zone` must be installed to `conf.d/` before the vhost that references it. One item is deliberately deferred with its reason in the note rather than guessed at: match-cache eviction, which gates on verified backups. Owner-auth covers *who may call* the mutating routes, but not what an anonymous caller can make the api **do**; a ten-lane sweep found **22 findings**, one of which (the Steam API key written to our own logs in cleartext on any fetch failure) is live on the dev box today rather than gated on launch. Three patterns explain nearly all of it: an invariant enforced by hand at 27 call sites and missed at 3, caches that are absent/bypassable/never-written turning cheap requests into expensive ones, and validation that is strong wherever a DTO exists and absent wherever a raw `@Param` binding does. Sharpest individual items: an unvalidated `tier` param path-traverses into four trusted upstreams (confirmed live), `/og/home.png` is ~70 ms of uncached synchronous main-thread raster with no parameters at all, `/status` publishes raw exception messages in a 200 body, one shared Steam reservoir means a sustained attacker drains the whole daily quota in ~5.5 h, and `/steam/game/:appid/description` re-fetches from Steam forever because its write targets a row that does not exist. A reported "repeated query param crashes two endpoints" finding was **refuted by probe** and recorded so it is not re-derived. Original four: three LoL routes (`baselines`, `narrative`, `narrative/lifetime`) reach `resolveSummoner` without the owner allowlist that 27 other call sites enforce, so any summoner name can be resolved against Riot and written to `Summoner`; `GET /lol/matches/:matchId` has no allowlist at all and inserts a cache row per enumerable matchId, making the api a free public Riot proxy; `/og` renders a PNG per request behind an `s-maxage` header addressed to a shared cache that does not exist; and no inbound rate limiting exists at either layer, which is the multiplier on the other three. F-1's shape is the `excludeRemakes` lesson repeating — an invariant held by hand-written call sites, missed when two newer services landed — so the fix is a boundary guard plus a structural lint, not a 28th inlined check. → [api-exposure-audit.md](ops/api-exposure-audit.md)
- **Branch protection on `main` (pre-deploy)** — **deliberately off, decided 2026-07-26.** The CI gate became capable of failing on 2026-07-25 (F-1), but the repo is pushed to directly and required status checks would block that, forcing a PR flow. Nothing is in production, so CI is accepted as a post-hoc alarm rather than a merge gate. **Trigger: enable it as part of the pre-deploy sweep**, alongside owner-auth and hosting, at which point direct-push stops being appropriate anyway. Do not raise this as a finding in the meantime — it is a decision, not a gap. → [hosting.md](ops/hosting.md)
- **Owner auth (pre-deploy)** — **chunks 1 and 2 both shipped 2026-08-13; the launch gate is closed.** Chunk 1: `Session` model, `AuthModule` with `login`/`callback`/`logout`/`viewer`, `OwnerGuard`, `Viewer` in `@vyoh/shared`. Chunk 2: `@UseGuards(OwnerGuard)` on all four mutating routes (three status POSTs + the per-account sync trigger — that is the api's entire write surface), `apps/web/src/auth/`, an unlinked `noindex` `/login`, locked-with-tooltip status controls, a nav owner badge, and 401 → re-lock in the status mutations. A `conventions.spec.ts` lint pins the decorator to each route by name. Verified live: 401 without a session, 200/201 with one, CORS credentials echoed on both. **Chunk 3 is prod wiring, not code** — a separate prod GitHub OAuth app, the env values on the box, cookie `domain`, README section — and lands with the VPS; the compose gap found 2026-08-20 is fixed and `compose.prod.yaml` requires all four auth vars at boot. → [owner-auth.md](ops/owner-auth.md)

## Adjacent maintenance (sub-session each)

### LoL surfaces

- Riot-investigation parked tail: per-account cache TTL self-healing, re-derive `reservoirIncreaseInterval` when prod-tier key lands, sync fairness if accounts ever run in parallel. → [riot-investigation-2026-05-07.md](lol/riot-investigation-2026-05-07.md)

### Cross-cutting

- **Iterate `excludeBlipSessions()` in the remaining `SteamPlaySession` readers** — added 2026-09-16. The sessions page floors single-tick rows through the shared helper; `home-session-lengths`, `home-day-split`, `home-today`, `home-activity-intensity`, `steam-moments` and the portrait still aggregate raw closed rows. A zero-minute row is harmless to a sum or a day split, so this is hygiene under the domain-invariant rule, not a visible defect; pair it with a `conventions.spec.ts` lint if a second reader ever needs the floor for a visible reason. → [play-sittings.md](steam/play-sittings.md)
- **Web audit — chunked items closed** — W1 (formatter dedupe) shipped 2026-09-02; W2 (sheen recipe → `<SheenOverlay>` in `components/`) and W3 (`match-detail-recap-tab.tsx` 908 → 78 lines, sub-components in `lol/matches/recap/`) shipped 2026-09-06; W5 shipped with Start; W4 parked. → [audit-web-structure.md](cross-cutting/audit-web-structure.md)
- **API audit — chunked items closed** — A1 (global fallback filter: Steam, Prisma and unknown throws answer one `{ statusCode, message }` shape, 5xx logged with stack) shipped 2026-09-06; A2 closed by owner-auth; A3/A4 parked. → [audit-api-structure.md](cross-cutting/audit-api-structure.md)
- **Biome 1.9.4 → 2.x** — planned, not started; evaluated and deferred 2026-07-26 with a full in-repo measurement (599 errors / 79 warnings over 1,156 files). **The motivation is staying on the maintained line, not fixing bugs**: the two correctness rules that justified the bump both fail triage — all 10 `useHookAtTopLevel` hits are `forwardRef` false positives in one file, and all 9 `noUnsafeOptionalChaining` hits are test-only cosmetics guarded by an assertion on the line above. Blocked on a decision (stage behind disabled rules vs one large commit) and on Biome 2 failing to parse `index.css`, which is a capability regression against v1. Chunk plan and revisit triggers in the note; do not re-propose on version-currency grounds alone. → [biome-2-migration.md](ops/biome-2-migration.md)
- Re-measure MatchWindowProvider + ChampionsPage memoization fixes in host Chrome (devcontainer can't). Not a coding task. → [perf-baseline.md](cross-cutting/perf-baseline.md)
- Frontend-2026 KB gaps — Round 1: A's head-baseline portion SHIPPED 2026-05-23 (c6c3720; OG image SHIPPED 2026-06-07 via the dynamic `/og/home.png` endpoint), A's LCP-element fetchpriority still pending re-measure; B (React Compiler) SHIPPED 2026-05-23 (0e8800c); C (app-root + widget error boundaries) SHIPPED 2026-06-23 — app-root tier + widget primitives + palette/splash + `ChartBoundary` on 11 chart leaves (two commits); route tier (E) SHIPPED 2026-07-27 via Start chunk 5's router-level `defaultErrorComponent`/`defaultPendingComponent`; D (RUM backend) post-launch. Round 2 added 2026-05-22 — F (color-scheme + container-query pilot), G (charting decision tree docs), H (Radix import consolidation) — all still ready. Round 4 added 2026-05-23: M (static-metadata `staleTime: Infinity`) SHIPPED 2026-05-23 (c68026a); most patch-keyed hooks were already pinned by the 2026-05-21 static-metadata arc. Round 5 added 2026-05-23 (frameworks sweep): **N (route loader pilot on match-detail) SHIPPED 2026-07-26 — non-blocking `prefetchQuery` rather than the specced blocking `ensureQueryData`, because `match-row` awaits `navigate()` inside `startViewTransition` and the route has no pending/error component; the fan-out in P inherits that constraint; ~~O (`head()` localhost bug fix)~~ SHIPPED 2026-07-26 as chunk 2 of the Start migration — 65 re-declared sites (not the 20+ estimated) consolidated into `apps/web/src/lib/api-url.ts`, split fetch-side vs render-side, with a structural lint against re-declaration; ~~P (loader + `head()` fan-out across detail routes)~~ — `head()` half SHIPPED 2026-06-07 alongside [og-image-pipeline.md](cross-cutting/og-image-pipeline.md) C1–C4 (champion + profile + Steam game + index.html baseline), loader half still pending after N**. Gap #5 corrected — splash isn't LCP. → [frontend-2026-gaps.md](cross-cutting/frontend-2026-gaps.md)

### Ops

- **`typescript` 6 → 7** — blocked upstream, re-tested 2026-07-26. TS 7.0 ships `tsc` without the programmatic compiler API `@nestjs/cli` requires, and `7.1.0-dev.20260725.1` still fails identically, so the Nest error's own "returns in 7.1" prediction is not holding. Trigger is a capability, not a version: retry when `pnpm --filter @vyoh/api build` succeeds under a stable 7.x. → [state-review-2026-07-25.md](cross-cutting/state-review-2026-07-25.md)
- CodeQL SAST evaluation — deferred from the 2026-05-14 security baseline; freelance-signal layer, not threat-model-justified. Its auth-surface trigger fired 2026-08-13; sequenced post-launch per [pre-launch-sweep.md](ops/pre-launch-sweep.md). → [security.md](ops/security.md)
- Dependency refresh cadence — the 2026-07-25 sweep (41 advisories → 1, CI audit gate un-broken) showed overrides pinned to fixed floors stop covering advisories as those widen. Overrides now mirror the advisory ranges and carry a re-derivation date; re-derive on each sweep rather than only adding new entries. Re-derived 2026-09-01, 2026-09-02 and **2026-09-16**, the last of those unbreaking a red audit job with `sharp` 0.35.4, `js-yaml` 4.3.2, `adm-zip` past its own floor, and the block's first override against an exact upstream pin (`multer` under `@nestjs/platform-express`). One accepted moderate remains (`file-type@16` via `@jimp/core`), clearing only when `node-vibrant` moves. → [security.md](ops/security.md)
- Folder-structure cleanup — Chunks 1 + 2 shipped 2026-05-14 (`lol-analytics.service.ts` extracted; `lol/_shared/` split into 6 non-asset buckets). Asset buckets deferred to the runtime-proxy pivot; Chunk 3's web half happened organically (`apps/web/src/steam/` carries nine feature subfolders), and the api half shipped 2026-09-06 as five move-only commits, so `apps/api/src/steam/` is now seven seam folders (`client`, `presence`, `store`, `portrait`, `achievements`, `library`, `enrichment`) around seven root files — Chunk 3 closed; Chunk 4 (cross-domain `_assets/`) only if TFT lands. → [folder-structure-cleanup.md](ops/folder-structure-cleanup.md)

## Unpromoted vNext top-tier candidates

Cross-account unified identity · "Same day, last year" · match annotations · weekly markdown digest · PDF/image export of match detail · Discord webhook · drag-to-reorder Profile. (ARAM dashboard shipped 2026-06-22 as D.7; the View Transitions spike was overtaken by the 2026-05-24 VT migration; the bundle-budget half of ten-pick #9 shipped as the `size-limit` CI job and the Web Vitals trend dashboard is sequenced behind the post-launch RUM backend.) → [vnext-ideas.md](cross-cutting/vnext-ideas.md)

## Case-study backlog

No active write-ups queued. Shipped inventory: [../case-studies/](../case-studies/) (21 case studies as of 2026-07-25). Topics catalog: [case-study-topics.md](cross-cutting/case-study-topics.md).
