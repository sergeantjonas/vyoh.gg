// Champion accent colors moved to @vyoh/shared so the api can resolve them
// too (OG-card ridge backgrounds). Re-exported here so web call sites keep
// importing from the assets barrel they always have.
export { championTheme, type ChampionAsset } from "@vyoh/shared";
